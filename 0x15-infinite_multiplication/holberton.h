#ifndef HEADER_FILE
#define HEADER_FILE
#include <stdio.h>
#include <unistd.h>
#include <ctype.h>
#include <stdlib.h>
#include <string.h>
int _putchar(char c);
int _strlen(char *s);
int _isdigit(int c);
void print_ans(int *a, int i);
int validation(int argc, char *argv[]);
#endif
